# LLM Wiki 架构 - 坐标：世界物理版图中的中国 (写作助手版)

此架构定义了由 LLM 维护的维基（Wiki）的结构和操作，旨在作为**写作助手**，协助作者完成《坐标：世界物理版图中的中国》一书的编写。

## 核心理念
维基是一个**持续复合的知识库**。LLM 负责处理繁琐的资料整理、交叉引用、冲突检测和日志记录（做苦力），而作者负责引导分析、提出问题和决定书籍的最终呈现（做导演）。

## 三层架构

1. **原始来源 (Raw Sources)**：`/sources` 目录。存放作者收集的原始史料、论文、笔记等。这些文件是**不可变**的，LLM 只读不写。
2. **维基知识库 (The Wiki)**：`/wiki` 目录。由 LLM 生成和维护的 Markdown 文件集合，是介于原始资料和最终成书之间的“中间件”。
3. **架构与规范 (The Schema)**：即本文件 `AGENTS.md`。定义了维基的结构和 LLM 的工作流。

## 详细目录结构

- `/sources`: 原始资料。
- `/wiki`: 知识库。
  - `/wiki/index.md`: 面向内容的目录，LLM 在每次摄取时更新。
  - `/wiki/log.md`: 按时间顺序记录的日志。
  - `/wiki/entities`: 核心人物（如墨子）、组织、仪器的页面。
  - `/wiki/concepts`: 物理概念、范式转移的页面。
  - `/wiki/summaries`: 源文档的总结页面。
  - `/wiki/drafts`: 章节草稿、大纲和写作素材（新增，用于辅助写作）。
- `/website/docs`: 最终的书籍章节（Docusaurus 结构，最终产品）。

## 核心操作流

### 1. 摄取 (Ingest) - 研究阶段
当新源文件添加到 `/sources` 时：
1. LLM 阅读源文件，提取与本书主题（中国在物理版图中的坐标）相关的关键信息。
2. 在 `/wiki/summaries/` 中创建总结页面，**特别注明可用于写作的论点或素材**。
3. 更新 `/wiki/index.md`。
4. 创建或更新相关的实体和概念页面，建立双向链接。
5. 在 `/wiki/log.md` 中记录该操作。

### 2. 辅助写作 (Drafting) - 创作阶段
1. **大纲共创**：作者提出章节设想，LLM 根据维基中的积累，协助生成详细的写作大纲。
2. **素材调取**：在写作特定章节时，作者可要求 LLM 从维基中检索所有相关的史料和论据。
3. **草稿生成**：在 `/wiki/drafts` 中编写章节初稿，供作者审阅和修改。

### 3. 同步与发布 (Publish & Sync) - 成书阶段
1. 当草稿在 `/wiki/drafts` 中被作者确认后，LLM 协助将其转化为 Docusaurus 格式，并放入 `/website/docs` 对应的章节中。
2. 协助维护 `sidebars.js` 以确保书籍目录结构正确。

### 4. 检查 (Lint) - 健康检查
定期要求 LLM 检查维基和草稿：
- 寻找页面间的矛盾之处。
- 发现孤立页面（无入站链接）。
- 寻找被提及但尚未建立独立页面的重要概念。
- 检查草稿中的论据是否在维基中有坚实的来源支撑。

---
这是为了支持书籍写作而定制的架构。让我们在实践中不断优化它。
